{
  "name": "vibewarz-oss",
  "private": true,
  "version": "0.0.0",
  "description": "vibewarz OSS — JS workspace root (React replay-viewer package + standalone Vite viewer app)",
  "packageManager": "pnpm@10.30.2",
  "scripts": {
    "build": "pnpm -r run build",
    "build:viewer": "pnpm -F @vibewarz/replay-viewer build && pnpm -F viewer build",
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck",
    "changeset": "changeset",
    "release": "pnpm -F @vibewarz/replay-viewer build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9"
  }
}
